/**
 * Asqav Shadow AI Capture - content script stub.
 *
 * Default capture mode is metadata-only: the service worker captures the
 * navigation event and never reads page contents. This content script is a
 * deliberate no-op placeholder so the manifest can extend to a prompt-body
 * opt-in mode without re-requesting the extension's permission grant.
 *
 * Do not add prompt-body capture logic here without a corresponding policy
 * gate and an explicit admin opt-in surfaced on the options page.
 */
(() => {
  // Intentionally empty.
})();
