/**
 * Asqav Shadow AI Capture - content script stub.
 *
 * v0 runs in metadata-only mode: the service worker captures the navigation
 * event and never reads page contents. This content script is intentionally a
 * no-op placeholder so that the v0.5 prompt-body opt-in (off by default) can
 * be added without re-issuing the extension's permission grant.
 *
 * Do not add prompt-body capture logic here without a corresponding policy
 * gate and an explicit admin opt-in surfaced on the options page.
 */
(() => {
  // Intentionally empty.
})();
